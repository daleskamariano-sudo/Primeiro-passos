const botao = document.querySelector("button");
botoes.forEach(function(botao) {
    let curtiu = false;
botao.addEventListener("click", botaoClicado);
function botaocjicado () { 
     consolete.log ( "fui clicado");
     let texto = botao.querySelector("span");
     if (curtiu === false) {
        
     }
     texto.textoContent++;
}
});
